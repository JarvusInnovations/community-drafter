/**
 * Source for the `signatories-axi` admin CLI (an AXI-style tool over the admin
 * API), built into `skills/signatories-axi/` per the `axi-skills` skill. Empty
 * scaffold — populated once the admin API surface exists to drive.
 */
export const CLI_PACKAGE_NAME = "@signatories/cli";
