{
  "name": "@community-drafter/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "oxlint src",
    "format": "oxfmt src",
    "format:check": "oxfmt --check src",
    "typecheck": "tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "@community-drafter/shared": "workspace:*",
    "@fastify/env": "^7.0.0",
    "fastify": "^5.12.5",
    "fastify-plugin": "^6.0.0",
    "gitsheets": "^2.6.0",
    "pino-pretty": "^13.1.3"
  }
}
